#!/bin/bash
#
# Adapted from https://github.com/kr/heroku-buildpack-go
#
# usage: bin/compile <build-dir> <cache-dir>

set -eo pipefail

source "${OPENSHIFT_REPO_DIR}/lib/util"
cd $OPENSHIFT_REPO_DIR
curl https://install.meteor.com/ | sh
